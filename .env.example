# =============================================================================
# Bússola de Vendas - Variáveis de ambiente
# Copie para .env e preencha os valores (nunca commite o .env).
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (opcional por enquanto)
# -----------------------------------------------------------------------------
# Project URL e Anon Key (Settings > API no Supabase).
NEXT_PUBLIC_SUPABASE_URL=https://rmszekgblrchqnbgzwwz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# Banco de dados (obrigatório)
# -----------------------------------------------------------------------------
# URL de conexão Postgres (Supabase).
# Use a Connection pooling (Session) em Settings > Database no Supabase.
# Preencha REGION (ex.: us-east-1) e a senha do banco.
# Ex (ilustrativo):
# postgresql://postgres.rmszekgblrchqnbgzwwz:SUA_SENHA_DO_BANCO@aws-0-REGION.pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=postgresql://postgres.rmszekgblrchqnbgzwwz:SUA_SENHA_DO_BANCO@aws-0-REGION.pooler.supabase.com:6543/postgres?sslmode=require

# -----------------------------------------------------------------------------
# Foto de perfil (upload na Vercel)
# -----------------------------------------------------------------------------
# Token para Vercel Blob (Settings > Storage no dashboard Vercel).
# Se não definir, o upload de foto nas Configurações retornará erro 503.
# BLOB_READ_WRITE_TOKEN=

# -----------------------------------------------------------------------------
# Autenticação (opcional)
# -----------------------------------------------------------------------------
# Por padrão, o endpoint `/api/auth/register` é bloqueado.
# Se você quiser liberar cadastro público (não recomendado), use:
# ALLOW_PUBLIC_SIGNUP=true

# -----------------------------------------------------------------------------
# Hotmart (webhook)
# -----------------------------------------------------------------------------
# Chave secreta do webhook (configurada no painel Hotmart ao cadastrar a URL).
# Usada para validar o header x-hotmart-signature (HMAC-SHA256). Não expor no frontend.
# HOTMART_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Importação do Notion (opcional)
# -----------------------------------------------------------------------------
# Diretório do export do Notion. Se vazio, usamos o padrão:
# `./bussola completa com exemplos ` (com espaço no final, como está no repo).
# NOTION_EXPORT_DIR=
#
# Para importar ideias de conteúdo (CSV), informe qual usuário (email) será o dono:
# IMPORT_USER_EMAIL=
